# BarberOS — Universal Men's Barbershop Automation Workflow

> **نسخه:** ۲.۰.۰ (بازنویسی کامل، جایگزین v1.1.0)
> **نوع:** Universal / Platform-Agnostic
> **زبان اصلی:** فارسی (`fa-IR`)
> **منطقه‌ی زمانی پیش‌فرض:** `Asia/Tehran`
> **تقویم:** جلالی (شمسی) برای نمایش، ذخیره‌سازی زمان به‌صورت UTC
> **تاریخ انتشار:** سپتامبر ۲۰۲۶

---

## فهرست

1. [هدف پروژه](#1-هدف-پروژه)
2. [مفاهیم و واژگان پایه](#2-مفاهیم-و-واژگان-پایه)
3. [مدل داده‌ی مفهومی](#3-مدل-داده-مفهومی)
4. [مدل ظرفیت و زمان](#4-مدل-ظرفیت-و-زمان)
5. [ماشین حالت نوبت](#5-ماشین-حالت-نوبت)
6. [سیاست رزرو و همزمانی](#6-سیاست-رزرو-و-همزمانی)
7. [CRM مشتری](#7-crm-مشتری)
8. [امتیاز ریزش (Risk Score)](#8-امتیاز-ریزش-risk-score)
9. [رزرو پیش‌بینانه (Predictive Rebooking)](#9-رزرو-پیشبینانه-predictive-rebooking)
10. [بازیابی Slot خالی و لیست انتظار](#10-بازیابی-slot-خالی-و-لیست-انتظار)
11. [مدیریت No-Show](#11-مدیریت-no-show)
12. [برنامه‌ی معرفی (Referral)](#12-برنامه-معرفی-referral)
13. [مدل پرداخت](#13-مدل-پرداخت)
14. [موتور اتوماسیون و پیام‌رسانی](#14-موتور-اتوماسیون-و-پیامرسانی)
15. [هوش تجاری (BI) و KPI](#15-هوش-تجاری-bi-و-kpi)
16. [هوش مصنوعی (AI)](#16-هوش-مصنوعی-ai)
17. [گزارش صبحگاهی مالک](#17-گزارش-صبحگاهی-مالک)
18. [معماری سیستم](#18-معماری-سیستم)
19. [چندمستأجری و پیکربندی](#19-چندمستاجری-و-پیکربندی)
20. [نقش‌ها و احراز هویت](#20-نقشها-و-احراز-هویت)
21. [مهاجرت داده و راه‌اندازی](#21-مهاجرت-داده-و-راهاندازی)
22. [امنیت و حریم خصوصی](#22-امنیت-و-حریم-خصوصی)
23. [کیفیت، پشتیبان‌گیری و استقرار](#23-کیفیت-پشتیبانگیری-و-استقرار)
24. [پشته‌ی فنی (Stack)](#24-پشته-فنی-stack)
25. [نقشه‌ی راه](#25-نقشه-راه)
26. [پیوست‌ها](#26-پیوستها)

---

## 1. هدف پروژه

BarberOS صرفاً یک سیستم نوبت‌دهی نیست؛ یک **چرخه‌ی کامل مدیریت و رشد** آرایشگاه مردانه است:

```text
Discover → Book → Confirm → Remind → Visit → Pay → Feedback → Rebook → Retain → Reactivate → Refer
```

این چرخه نه‌تنها در سطح محصول، بلکه در **معماری فنی** نیز باید قابل‌ردیابی باشد (بخش ۱۸).

### ۱.۱ اصول طراحی (Non-Negotiable)

1. **AI تصمیم می‌گیرد، آمار محاسبه می‌کند، انسان تأیید می‌کند.** AI هرگز عدد نسازد و هرگز بدون تأیید، به مشتری پیام نفرستد.
2. **هسته مستقل از سرویس‌ها:** تمام اتصالات خارجی از طریق Adapter/Interface (بخش ۱۸.۳).
3. **یک منبع حقیقت (Single Source of Truth):** تقویم و وضعیت نوبت فقط در یک جا (DB) معتبر است.
4. **چندمستأجری از روز اول:** هر رکورد `tenant_id` دارد؛ اضافه‌کردن آرایشگاه جدید فقط پیکربندی است، نه کدنویسی.
5. **زمان‌محور بودن بحرانی:** همه‌ی محاسبات زمان بر اساس `Asia/Tehran` و تقویم جلالی انجام می‌شود.

### ۱.۲ مخاطبان سیستم

| نقش | شرح |
|---|---|
| مشتری | رزرو، دریافت یادآوری، مدیریت نوبت خود |
| پذیرش / اپراتور | رزرو دستی، جابجایی نوبت، چک‌این |
| آرایشگر | مشاهده‌ی تقویم خود، ثبت شروع/پایان سرویس |
| مالک / مدیر | داشبورد، BI، AI، تأیید کمپین‌ها، پیکربندی |

---

## 2. مفاهیم و واژگان پایه

برای جلوگیری از ابهام، **برچسب کانونی (Canonical)** هر مفهوم این‌جاست. رابط کاربری می‌تواند ترجمه‌ی فارسی نمایش دهد، اما در دیتابیس و API فقط همین برچسب‌ها به کار می‌رود.

| برچسب کانونی | تعریف |
|---|---|
| `appointment` | یک نوبت رزروشده برای یک مشتری، نزد یک آرایشگر، در یک بازه‌ی زمانی |
| `slot` | بازه‌ی زمانی خالیِ قابل‌رزرو در تقویم یک آرایشگر |
| `service` | خدمت ارائه‌شده (اصلاح، کوتاهی و …) با مدت‌زمان و قیمت |
| `barber` | آرایشگر |
| `customer` | مشتری (شخص حقیقی با شماره‌ی موبایل یکتا) |
| `visit` | یک `appointment` در وضعیت `completed` (مراجعه‌ی انجام‌شده) |
| `tier` | سطح ارزش مشتری: `regular` / `vip` |
| `lifecycle` | مرحله‌ی چرخه‌ی عمر مشتری: `new` / `active` / `at_risk` / `inactive` / `churned` |
| `returning` (برگشتی) | مشتری با **حداقل ۲** مراجعه‌ی `completed` |
| `no_show` | نوبتِ `confirmed` که مشتری بدون لغو در آن حاضر نشده است |
| `tenant` | یک آرایشگاه مستقل در سیستم چندمستأجری |

> **اصلاح نسبت به v1.1:** `vip` از فهرست وضعیت‌ها حذف و به‌عنوان بُعدِ مستقل `tier` تعریف شد (بخش ۷). مفهوم «برگشتی» نیز صریح تعریف شد.

---

## 3. مدل داده‌ی مفهومی

### ۳.۱ موجودیت‌های اصلی

| موجودیت | فیلدهای کلیدی |
|---|---|
| `tenant` | id, config |
| `customer` | id, tenant_id, mobile (یکتا در tenant), name, tier, lifecycle, risk_score, joined_at |
| `barber` | id, tenant_id, name, skills, active |
| `service` | id, tenant_id, name, duration_minutes, price_rial, buffer_minutes, active |
| `appointment` | id, tenant_id, customer_id, barber_id, service_id, start_at (UTC), status, created_at, updated_at |
| `availability_override` | barber_id, date, closed / custom hours |
| `holiday` | tenant_id, jalali_date, reason |
| `message` | id, tenant_id, appointment_id, customer_id, event, channel, template, status, idempotency_key, sent_at |
| `waitlist_entry` | id, tenant_id, customer_id, slot params, score, status |
| `payment` | id, tenant_id, appointment_id, amount_rial, status, psp_ref |
| `referral` | id, tenant_id, referrer_id, referee_id, code, status |
| `event_log` | id, tenant_id, type, payload (JSON), created_at |
| `ai_run` | id, tenant_id, task, input_digest, output (JSON), approved_by, created_at |

### ۳.۲ قواعد کلی مدل

- **مبالغ پول:** همیشه به **ریال** و به‌صورت `integer` ذخیره شود (اجتناب از خطای اعشار). نمایش به تومان (`÷۱۰`) است.
- **زمان‌ها:** ذخیره به `TIMESTAMPTZ` (UTC)؛ تبدیل به تهران/جلالی فقط در لایه‌ی نمایش و زمان‌بندی.
- **شماره موبایل:** نرمال‌سازی یکنواخت (`+98` / `09…`) و یکتا در هر `tenant` برای جلوگیری از پروفایل تکراری (بخش ۲۰.۳).

---

## 4. مدل ظرفیت و زمان

> **اصلاح نسبت به v1.1:** در v1.1 فقط «ساعات کاری» وجود داشت. ظرفیت واقعی از **اشتراک** سه لایه به دست می‌آید.

### ۴.۱ لایه‌های ظرفیت

```text
ظرفیتِ آرایشگر در روز = (ساعات کاری آرایشگاه) ∩ (دسترسی آرایشگر) ∩ (تقویم/تعطیلات) − (نوبت‌های فعال) − (بافر)
```

| لایه | منبع | توضیح |
|---|---|---|
| ساعات کاری آرایشگاه | `tenant.working_hours` | به‌ازای هر روز هفته (جلالی) |
| دسترسی آرایشگر | `barber.working_hours` + `availability_override` | مرخصی، نیمه‌وقت، روز خاص |
| تعطیلات | `holiday` | تعطیلات رسمی ایران (پویا) + تعطیلی خاص آرایشگاه |
| نوبت‌های فعال | `appointment` | وضعیت‌های `confirmed/checked_in/in_progress` |
| بافر | `service.buffer_minutes` یا `barber.buffer_minutes` | زمان آماده‌سازی بین دو مشتری |

### ۴.۲ پارامترهای زمان‌بندی

| پارامتر | پیش‌فرض | توضیح |
|---|---|---|
| `slot_granularity_minutes` | ۱۵ | دانه‌بندی شروع slotها |
| `default_buffer_minutes` | ۵ | بافر پیش‌فرض بین نوبت‌ها |
| `min_lead_minutes` | ۳۰ | حداقل فاصله‌ی رزرو تا شروع (مثلاً نمی‌شود ۵ دقیقه قبل رزرو کرد) |
| `max_advance_days` | ۳۰ | حداکثر افق رزرو |
| `min_notice_cancellation_hours` | ۳ | حداقل ساعت‌های مجاز برای لغو بدون جریمه |
| `no_show_grace_minutes` | ۱۵ | مهلت چک‌این پس از شروع، قبل از ثبت No-Show |
| `overbooking_enabled` | `false` | همیشه خاموش؛ اگر روشن هم شد باید صریح باشد |

### ۴.۳ تقویم جلالی

- کتابخانه‌ی تبدیل: `jalaali-js` (سرور) و `date-fns-jalali` / `Intl` با locale `fa-IR` (کلاینت).
- **جمعه روز تعطیل پیش‌فرض** است؛ تعطیلات رسمی (عید نوروز، روزهای تعطیل و…) در جدول `holiday` به‌صورت پویا نگهداری و سالانه به‌روزرسانی می‌شود.
- فرمت نمایش تاریخ به مشتری: `۱۴۰۵/۰۶/۱۸`.

### ۴.۴ خدمت و آرایشگر

- هر `service` دارای `duration_minutes` است؛ زمان اتمام نوبت = `start + duration + buffer`.
- هر `barber` دارای لیست `skills` (خدماتی که می‌تواند انجام دهد) است؛ هنگام انتخاب آرایشگر فقط آرایشگرانِ دارای مهارتِ خدمت نمایش داده می‌شوند.
- **قاعده‌ی طلایی رندر تقویم:** فقط بازه‌هایی که `end - start ≥ duration + buffer` باشند، slot معتبرند.

---

## 5. ماشین حالت نوبت

> **اصلاح نسبت به v1.1:** در v1.1 حالت‌های نوبت تعریف نشده بود. این بخش قلب سیستم است.

### ۵.۱ وضعیت‌ها

```text
requested → confirmed → checked_in → in_progress → completed
     └──────────────→ cancelled
     └──────────────→ no_show
```

| وضعیت | معنا | نوع |
|---|---|---|
| `requested` | درخواست ثبت شده، هنوز تأیید نشده (در انتظار تأیید/پرداخت) | فعال موقت |
| `confirmed` | رزرو قطعی و قفل‌شده در تقویم | فعال |
| `checked_in` | مشتری حاضر شده | فعال |
| `in_progress` | سرویس در حال انجام | فعال |
| `completed` | انجام‌شده (مراجعه‌ی موفق) | پایانی |
| `cancelled` | لغو شده (slot آزاد می‌شود) | پایانی |
| `no_show` | بدون حضور و بدون لغو | پایانی |

> `waitlisted` وضعیت نوبت **نیست**؛ یک موجودیت جداگانه (`waitlist_entry`) است (بخش ۱۰).

### ۵.۲ جدول گذارها

| از | به | رویداد | بازیگر(ها) | شرط/گارد | اثرات جانبی |
|---|---|---|---|---|---|
| — | `requested` | `appointment.created` | مشتری، پذیرش | slot آزاد باشد؛ قواعد رزرو رعایت شود | رزرو موقت در تقویم؛ صدور رویداد |
| `requested` | `confirmed` | `appointment.confirmed` | مشتری (پرداخت)، پذیرش | در صورت نیاز به پیش‌پرداخت، پرداخت موفق | قفل slot؛ ارسال پیام تأیید |
| `requested` | `cancelled` | `appointment.cancelled` | مشتری، پذیرش | قبل از `min_notice_cancellation_hours` | آزادسازی slot؛ تریگر Waitlist |
| `confirmed` | `cancelled` | `appointment.cancelled` | مشتری، پذیرش، آرایشگر | اگر دیر باشد → جریمه/ضبط پیش‌پرداخت (قابل پیکربندی) | آزادسازی slot؛ تریگر Waitlist؛ پیام اطلاع‌رسانی |
| `confirmed` | `no_show` | `appointment.no_show` | سیستم، پذیرش | عبور از `no_show_grace_minutes` بدون چک‌این | ثبت no-show؛ ضبط پیش‌پرداخت؛ به‌روزرسانی مشتری؛ پیام پیگیری |
| `confirmed` | `checked_in` | `appointment.checked_in` | پذیرش، آرایشگر | — | شروع زمان حضور |
| `checked_in` | `in_progress` | `appointment.started` | آرایشگر | — | — |
| `in_progress` | `completed` | `appointment.completed` | آرایشگر، پذیرش | — | ثبت visit؛ به‌روزرسانی `lifecycle`؛ صدور درخواست بازخورد |
| هر وضعیت فعال | `rescheduled` | `appointment.rescheduled` | پذیرش، مشتری | وجود slot آزاد جدید | لغو نوبت قبلی + ساخت نوبت جدید با `parent_appointment_id` |

### ۵.۳ قواعد انتقال

- فقط گذارهای جدول بالا مجازند؛ هر گذار خارج از آن با خطای `InvalidTransition` رد می‌شود.
- هر گذار یک `event` با همان نام در `event_log` منتشر می‌کند (منبع موتور اتوماسیون).
- **جابجایی (Reschedule)** به‌صورت اتمیک انجام می‌شود: لغو قبلی و ساخت جدید در یک تراکنش.

---

## 6. سیاست رزرو و همزمانی

> **اصلاح نسبت به v1.1:** «جلوگیری از Double Booking» در v1.1 فقط یک جمله بود؛ این‌جا به‌عنوان مسئله‌ی همزمانی دیتابیس حل می‌شود.

### ۶.۱ تضمین یکتایی در سطح دیتابیس

1. **ایندکس یکتا (Partial Unique Index):** روی `(barber_id, start_at)` **فقط برای وضعیت‌های فعال** (`requested/confirmed/checked_in/in_progress`). این تضمین می‌کند دو نوبتِ هم‌زمان فعال برای یک آرایشگر وجود نداشته باشد.

```sql
CREATE UNIQUE INDEX uq_active_slot
  ON appointment (tenant_id, barber_id, start_at)
  WHERE status IN ('requested','confirmed','checked_in','in_progress');
```

2. **تراکنش اتمیک رزرو:** رزرو در یک تراکنش انجام می‌شود:
   - بررسی قفل در دسترس بودن آرایشگر (`SELECT ... FOR UPDATE` روی ردیف دسترسی، یا `pg_advisory_xact_lock` روی `(tenant_id, barber_id, slot)`)،
   - بررسی قواعد رزرو،
   - `INSERT` نوبت.
   - نتیجه: اگر دو درخواست هم‌زمان برای یک slot برسند، **دقیقاً یکی** موفق می‌شود؛ دیگری خطای `SlotUnavailable` می‌گیرد.

3. **کلید Idempotency در سمت کلاینت:** هر فرم رزرو یک `Idempotency-Key` ارسال می‌کند تا دابل‌کلیک منجر به دو رزرو نشود.

### ۶.۲ قواعد رزرو

| قاعده | توضیح |
|---|---|
| بازه‌ی مجاز | `now + min_lead_minutes ≤ start ≤ now + max_advance_days` |
| ظرفیت | `start` و `end` داخل ساعات کاری و دسترسی آرایشگر باشد و تعطیل نباشد |
| مدت | `end − start ≥ duration + buffer` |
| سقف نوبت فعال هر مشتری | قابل پیکربندی (پیش‌فرض: ۱ نوبت فعال در آینده) |
| ممنوعیت هم‌زمانی | یک آرایشگر در یک لحظه فقط یک نوبت فعال |

---

## 7. CRM مشتری

> **اصلاح نسبت به v1.1:** در v1.1 ابعاد «ارزش» و «وضعیت چرخه‌ی عمر» در یک لیست قاطی شده بودند. این‌جا به دو بُعد مستقل تفکیک می‌شوند.

### ۷.۱ دو بُعد مستقل مشتری

```text
tier      : regular ──→ vip          (بُعد ارزش)
lifecycle : new → active ⇄ at_risk → inactive → churned   (بُعد عمر)
```

- یک مشتری می‌تواند **هم‌زمان** `vip` و `at_risk` باشد (مشتری باارزش در آستانه‌ی ریزش) — دو فیلد جدا.
- **آستانه‌ی VIP:** پیش‌فرض `8` مراجعه‌ی `completed` (قابل پیکربندی: `vip_visit_threshold`). رسیدن به آستانه → رویداد `customer.vip_upgraded` → پیام تبریک + مزایا.

### ۷.۲ قواعد گذار `lifecycle`

| از | به | قاعده‌ی گذار |
|---|---|---|
| `new` | `active` | ثبت اولین مراجعه‌ی `completed` |
| `active` | `at_risk` | `risk_score ≥ 0.45` یا عبور از پنجره‌ی بازگشت بدون رزرو |
| `at_risk` | `active` | ثبت مراجعه‌ی `completed` یا رزرو جدید `confirmed` |
| `at_risk` | `inactive` | گذشت `churn_grace_days` از تاریخ بازگشتِ مورد انتظار بدون هیچ فعالیت |
| `inactive` | `churned` | گذشت `churn_days` (پیش‌فرض ۱۸۰ روز) بدون هیچ پاسخ/مراجعه |
| `inactive`/`churned` | `active` | بازگشت از طریق کمپین (Reactivation) یا مراجعه‌ی جدید |

> محاسبه‌ی گذارها به‌صورت **batch روزانه** اجرا می‌شود (نه لحظه‌ای)، تا مقادیر پایدار و قابل‌ممیزی باشند.

### ۷.۳ پروفایل مشتری

| فیلد | توضیح |
|---|---|
| مشخصات | نام، موبایل (یکتا)، تاریخ عضویت |
| آمار مراجعه | تعداد کل `completed`، تاریخ آخرین مراجعه، مجموع خرید (ریال) |
| فاصله‌ی مراجعات | **میانه** (median) فاصله‌ها — مقاوم به outlier — به‌همراه میانگین برای گزارش |
| ترجیحات | آرایشگر موردعلاقه، خدمت موردعلاقه، بازه‌ی زمانی ترجیحی (از روی تاریخچه استخراج می‌شود) |
| ابعاد | `tier` ، `lifecycle` ، `risk_score` |
| رفتار | `no_show_count` ، `cancellation_count` |

> **اصلاح:** «آرایشگر/خدمت موردعلاقه» در v1.1 بدون تعریف بود؛ این‌جا به‌صورت «بیشترین فراوانی در `completed`های اخیر» محاسبه می‌شود.

---

## 8. امتیاز ریزش (Risk Score)

> **اصلاح نسبت به v1.1:** در v1.1 عدد (۰.۱۵ تا ۰.۹۵) بود ولی فرمول و مدیریت داده‌ی کم نبود.

### ۸.۱ فرمول

```text
الزام: تعداد مراجعات completed ≥ 3  (وگرنه به مسیر جایگزین ۸.۳ برو)
m      = median(فاصله‌های بین مراجعات، به روز)
elapsed = روزهای گذشته از آخرین مراجعه
risk_score = clamp( (elapsed − m) / m , 0 , 1 )
```

- `elapsed = m` → امتیاز `0` (دقیقاً سر وقت).
- `elapsed = 2·m` → امتیاز `1` (دوبرابر دیرتر از عادت = حداکثر ریزش).

### ۸.۲ نگاشت به دسته‌ها

| بازه‌ی `risk_score` | دسته |
|---|---|
| `< 0.15` | low |
| `0.15 – 0.45` | medium |
| `0.45 – 0.65` | high |
| `0.65 – 0.80` | very high |
| `> 0.80` | critical |

### ۸.۳ داده‌ی کم (n < 3)

برای مشتری با کمتر از ۳ مراجعه، «میانگینِ عادت» معنادار نیست. در این حالت:

```text
اگر elapsed > 2 × median_shop  →  کاندید at_risk
وگرنه  →  ریسک نامشخص (null)، محاسبه پس از رسیدن به ۳ مراجعه
```

که `median_shop` میانه‌ی فاصله‌ی مراجعات کل مشتریان `active` همان آرایشگاه است.

### ۸.۴ تعدیل‌گرها (Modifiers)

| رویداد | اثر |
|---|---|
| آخرین مراجعه `no_show` بوده | `+0.10` به امتیاز |
| دو لغو متوالی در ماه گذشته | `+0.10` به امتیاز |
| پاسخ مثبت به پیام یادآوری/پیشنهاد | `−0.10` از امتیاز |

نتیجه پس از تعدیل، در همان بازه‌ی `[0,1]` clamp می‌شود.

---

## 9. رزرو پیش‌بینانه (Predictive Rebooking)

> **اصلاح نسبت به v1.1:** مثال (۲۵ → ۲۲–۲۸) بود ولی قاعده‌ی استخراج نداشت.

### ۹.۱ پنجره‌ی بازگشت

```text
m = median(فاصله‌ی مراجعات، روز)
δ = max(2, round(0.1 × m))
window = [ m − δ , m + δ ]
```

| `m` | `δ` | پنجره |
|---|---|---|
| ۲۵ | ۳ | ۲۲ – ۲۸ (همان مثال v1.1) |
| ۴۰ | ۴ | ۳۶ – ۴۴ |
| ۱۰ | ۲ | ۸ – ۱۲ |

### ۹.۲ الگوریتم پیشنهاد

1. در هر اجرای روزانه، مشتریانی که `elapsed ∈ window` و `lifecycle ∈ {active, at_risk}` و هنوز نوبت آینده ندارند شناسایی می‌شوند.
2. برای هر کدام، slotهای آزادِ منطبق بر **ترجیحات** (همان آرایشگر، همان خدمت، بازه‌ی زمانی ترجیحی) جستجو می‌شود؛ در نبود تطابق کامل، نزدیک‌ترین گزینه‌ها.
3. پیام پیشنهاد با `{{suggestedSlots}}` ارسال می‌شود (تأیید انسانی، بخش ۱۶.۳).

---

## 10. بازیابی Slot خالی و لیست انتظار

> **اصلاح نسبت به v1.1:** در v1.1 فقط فهرست معیار بود؛ وزن، انقضا و سقف پیام نداشت.

### ۱۰.۱ چرخه‌ی بازیابی

```text
Cancelled Appointment → Released Slot → Waitlist Matching → Customer Ranking → Offer Slot → Booking
```

### ۱۰.۲ امتیازدهی صریح

هر معیار نرمال‌شده به `[0,1]` است و در امتیاز نهایی وزن دارد (وزن‌ها قابل پیکربندی):

| معیار | وزن پیش‌فرض | نرمال‌سازی |
|---|---|---|
| آرایشگر موردعلاقه | ۲۵٪ | تطابق آرایشگرِ slot با آرایشگرِ موردعلاقه (۰/۱) |
| `at_risk` | ۲۰٪ | `1` اگر `lifecycle = at_risk` |
| زمان موردعلاقه | ۲۰٪ | نزدیکی slot به بازه‌ی ترجیحی مشتری |
| VIP | ۱۵٪ | `1` اگر `tier = vip` |
| سابقه | ۱۰٪ | `min(visit_count / 10, 1)` |
| نزدیکی به بازگشت بعدی | ۱۰٪ | `1 − min(elapsed / m, 1)` |

```text
score = Σ (weight × criterion)
```

### ۱۰.۳ قواعد ارسال پیشنهاد (ضد اسپم و ضد قفل‌شدن صف)

| قاعده | مقدار پیش‌فرض |
|---|---|
| حداکثر پیشنهاد هم‌زمان برای هر slot | ۳ مشتری |
| انقضای هر پیشنهاد (`offer_expiry_minutes`) | ۱۲۰ دقیقه |
| حداکثر پیشنهاد برای هر مشتری در روز | ۱ |
| محدودیت کانال | مطابق ماتریس بخش ۱۴.۲ |

- با انقضای پیشنهاد (بدون پاسخ)، slot به نفر بعدیِ صف پیشنهاد می‌شود.
- اگر چند مشتری هم‌زمان پاسخ دهند، اولین **پرداخت موفق/تأیید** برنده است و بقیه خطای `SlotUnavailable` می‌گیرند.

---

## 11. مدیریت No-Show

> **اصلاح نسبت به v1.1:** در v1.1 فقط نام بود؛ اکنون تعریف، عواقب و اثر بر CRM دارد.

### ۱۱.۱ تعریف

```text
نوبتی در وضعیت confirmed که از start_at به اندازه‌ی no_show_grace_minutes (پیش‌فرض ۱۵) گذشته
و هیچ check-in ثبت نشده → سیستم وضعیت را به no_show می‌برد (یا پذیرش تأیید می‌کند).
```

### ۱۱.۲ عواقب (قابل پیکربندی در سطح tenant)

| سیاست | پیش‌فرض |
|---|---|
| ضبط پیش‌پرداخت | فعال (در صورت وجود پیش‌پرداخت) |
| افزایش `no_show_count` مشتری | همیشه |
| الزام پیش‌پرداخت برای رزرو بعدی | پس از ۲ no-show |
| پیام پیگیری («نبودید، رزرو مجدد؟») | ارسال در همان روز |

### ۱۱.۳ اثر در تحلیلیات

- `no_show` مستقیماً `risk_score` را بالا نمی‌برد اما به‌عنوان تعدیل‌گر (بخش ۸.۴) و در KPI (بخش ۱۵) اثر دارد.
- نرخ no-show به‌ازای مشتری، آرایشگر، خدمت و ساعت روز در BI گزارش می‌شود (الگوی «ساعت‌های پرریزش»).

---

## 12. برنامه‌ی معرفی (Referral)

> **اصلاح نسبت به v1.1:** مکانیزم کامل تعریف شد.

### ۱۲.۱ مکانیزم

1. هر مشتری یک `referral_code` کوتاه و یک **لینک/QR** اختصاصی دارد.
2. مشتری جدید در ثبت‌نام یا **نخستین رزرو** کد را وارد می‌کند → `referral` با `referrer` لینک می‌شود.
3. وضعیت پاداش: `pending → rewarded`؛ **اعطای پاداش پس از اولین مراجعه‌ی `completed` مشتریِ معرفی‌شده**.

### ۱۲.۲ پاداش (قابل پیکربندی)

| گیرنده | نوع پیش‌فرض |
|---|---|
| معرف | اعتبار/تخفیف (مثلاً ٪۱۵ روی خدمت بعدی) |
| معرفی‌شده | تخفیف خوش‌آمدگویی |

---

## 13. مدل پرداخت

> **اصلاح نسبت به v1.1:** `Pay` در چرخه و دیاگرام بود اما در هیچ فازی طراحی نشده بود.

### ۱۳.۱ حالت‌های پرداخت (قابل انتخاب در پیکربندی هر آرایشگاه)

| حالت | توضیح |
|---|---|
| `pay_at_visit` | پرداخت در محل (نقد/کارت) — پیش‌فرض برای شروع |
| `prepayment` | پیش‌پرداخت (درصد یا مبلغ ثابت) هنگام رزرو آنلاین |
| `full_online` | پرداخت کامل آنلاین |

### ۱۳.۲ پیاده‌سازی

- **درگاه:** IPG شاپرک از طریق Adapter `payments` (سرویس PSP داخلی). حالت `pay_at_visit` بدون درگاه کار می‌کند.
- **واحد:** مبالغ در DB به **ریال (integer)**؛ نمایش به تومان.
- **وضعیت پرداخت:** `pending → paid → refunded` (کامل/جزئی).
- **قواعد لغو/ریفاند:**
  - لغو قبل از `min_notice_cancellation_hours` → استرداد کامل پیش‌پرداخت.
  - لغو بعد از مهلت یا `no_show` → ضبط پیش‌پرداخت (قابل پیکربندی).

---

## 14. موتور اتوماسیون و پیام‌رسانی

> **اصلاح نسبت به v1.1:** در v1.1 فقط «متغیرهای تمپلیت» بود؛ اکنون رویدادها، ماتریس، Idempotency، منطقه‌ی زمانی و واقعیت کانال‌های ایران تعریف شده است.

### ۱۴.۱ رویدادها (Event Catalog)

```text
appointment.created / .confirmed / .cancelled / .checked_in / .completed / .no_show / .rescheduled
customer.vip_upgraded / customer.at_risk_detected / customer.reactivation
slot.released / waitlist.offer_sent / payment.succeeded / referral.rewarded
```

موتور اتوماسیون **مشترک رویدادها** است و بر اساس جدول زیر واکنش نشان می‌دهد.

### ۱۴.۲ ماتریس Trigger → Template → Channel → Timing

| رویداد | تمپلیت | کانال | زمان‌بندی | کلید Idempotency |
|---|---|---|---|---|
| `appointment.confirmed` | `confirm` | SMS | فوری | `appt:{id}:confirm` |
| یادآوری ۲۴ ساعت | `remind_24h` | SMS | `start − 24h` (به وقت تهران) | `appt:{id}:r24` |
| یادآوری ۳ ساعت | `remind_3h` | SMS | `start − 3h` | `appt:{id}:r3` |
| `appointment.cancelled` | `cancel_notice` | SMS | فوری | `appt:{id}:cancel` |
| `appointment.no_show` | `no_show_followup` | SMS | همان روز | `appt:{id}:nosh` |
| `appointment.completed` | `feedback_request` | SMS | `+۱h` | `appt:{id}:fb` |
| `customer.at_risk_detected` | `rebooking_suggest` | SMS | فوری (پس از تأیید انسانی) | `cust:{id}:rb:{run_id}` |
| `slot.released` | `slot_offer` | SMS | طبق قواعد ۱۰.۳ | `waitlist:{id}:offer` |
| `customer.vip_upgraded` | `vip_welcome` | SMS | فوری | `cust:{id}:vip` |
| `referral.rewarded` | `referral_reward` | SMS | فوری | `ref:{id}:reward` |

### ۱۴.۳ Idempotency و Retry

- جدول `message` با `idempotency_key` یکتا؛ اجرای تکراریِ همان رویداد، پیام تکراری تولید نمی‌کند.
- سیاست: `queued → sending → sent / failed`؛ شکست → retry با backoff (حداکثر ۳ بار) → در نهایت `failed` + هشدار به مالک.
- **هیچ پیامی بدون رکورد در `message` ارسال نمی‌شود** (حسابرسی کامل).

### ۱۴.۴ منطقه‌ی زمانی

- Cronها بر اساس **زمان محلی تهران** زمان‌بندی می‌شوند، نه UTC سرور.
- یادآوری `start − 24h` یعنی «۲۴ ساعتِ تقویمی قبل از شروع، به وقت تهران».
- ایران از ۲۰۲۲ تغییر ساعت فصلی ندارد؛ نگاشت UTC↔Tehran ثابت است (`+03:30`).

### ۱۴.۵ کانال‌های واقعی بازار ایران

| کانال | وضعیت | نکته |
|---|---|---|
| SMS | کانال اصلی | Provider: کاوه‌نگار / ملی‌پیامک و … (Adapter) |
| ربات تلگرام | کانال جایگزین/مکمل | برای مشتریانی که تلگرام دارند |
| واتساپ | عملاً غیرقابل استفاده در ایران | فقط در نسخه‌ی بین‌المللی |

> **نکته‌ی هزینه:** پیام فارسی = Unicode SMS؛ هر segment حدود **۷۰ کاراکتر** است (نه ۱۶۰). تمپلیت‌ها باید کوتاه طراحی شوند تا در ۱–۲ segment بمانند.

### ۱۴.۶ تمپلیت‌های پیام

متغیرها (پیش‌فرض v1.1 به‌علاوه‌ی موارد جدید):

```text
{{customerName}} {{businessName}} {{serviceName}} {{barberName}}
{{date}} {{time}} {{bookingUrl}} {{suggestedSlots}} {{feedbackUrl}}
{{price}} {{duration}} {{referralCode}} {{cancelUrl}}
```

نمونه‌ی تمپلیت (کوتاه، فارسی):

```text
{{customerName}} عزیز، نوبت شما در {{businessName}}:
{{serviceName}} با {{barberName}}
🗓 {{date}} ساعت {{time}}
جهت مدیریت: {{bookingUrl}}
```

> تمام تمپلیت‌ها در فایل پیکربندی (`templates`) با قابلیت بازنویسی برای هر آرایشگاه نگهداری می‌شوند.

---

## 15. هوش تجاری (BI) و KPI

> **اصلاح نسبت به v1.1:** گزارش‌ها بدون هدف عددی بودند؛ اکنون KPI با مقدار هدف تعریف شده و «برگشتی» صریح است.

### ۱۵.۱ گزارش‌ها (همه قطعی و SQL-محور، بدون LLM)

| گزارش | بازه |
|---|---|
| گزارش روزانه | امروز |
| گزارش هفتگی | هفته‌ی جاری |
| تحلیل ظرفیت | اشغال slotها در برابر ظرفیت |
| تحلیل درآمد | مجموع و میانگین به‌ازای مشتری |
| مشتریان جدید / برگشتی | new / returning (≥۲ مراجعه‌ی completed) |
| تحلیل No-Show و لغو | به‌ازای روز/ساعت/آرایشگر/خدمت |
| تحلیل خدمات | تعداد و درآمد هر خدمت |
| عملکرد آرایشگر | اشغال، درآمد، رضایت، no-show |

### ۱۵.۲ KPI با مقدار هدف

| KPI | فرمول | هدف |
|---|---|---|
| نرخ حضور (Show rate) | `(completed + in_progress) / confirmed` | ≥ ۹۰٪ |
| نرخ No-Show | `no_show / confirmed` | ≤ ۵٪ |
| نرخ لغو | `cancelled / confirmed` | ≤ ۱۰٪ |
| نرخ بازگشت ۶۰روزه | مشتریان با ≥۲ مراجعه در ۶۰ روز | ≥ ۵۰٪ |
| بهره‌برداری از ظرفیت | `ساعت‌های رزروشده / ساعت‌های قابل‌رزرو` | ≥ ۷۰٪ |
| درآمد به‌ازای مشتری | `مجموع درآمد / تعداد مشتریان` | رشد ماهانه |

---

## 16. هوش مصنوعی (AI)

> **اصلاح نسبت به v1.1:** اصل «AI عملی، نه شعاری» حفظ شده و گاردها، تفکیک از BI و قرارداد خروجی اضافه شده است.

### ۱۶.۱ اصل راهنما

```text
AI تصمیم می‌گیرد و اولویت‌بندی می‌کند؛ BI محاسبه می‌کند؛ انسان تأیید می‌کند.
```

- **BI (قطعی):** همه‌ی اعداد (درآمد، ظرفیت، no-show، عملکرد) از SQL می‌آیند. LLM هرگز عدد «حساب» نمی‌کند.
- **AI (استنتاج):** روی خروجی قطعی BI، اولویت‌بندی، رتبه‌بندی، روایت و متن پیشنهادی می‌سازد.

### ۱۶.۲ گاردها (Guardrails)

| گارد | توضیح |
|---|---|
| حداقل‌سازی PII | به LLM فقط شناسه‌های ناشناس + آمار می‌رود؛ نام/شماره فقط هنگام رندر نهایی در سمت سرور جای‌گذاری می‌شود |
| تأیید انسانی | خروجی AI که به مشتری پیام می‌شود، ابتدا «پیش‌نویس» است و مالک تأیید می‌کند |
| ضد توهم عددی | AI اجازه‌ی تولید اعداد مالی/آماری را ندارد؛ فقط مقادیر ورودی BI را ارجاع می‌دهد |
| اعتبارسنجی Schema | خروجی AI قبل از استفاده، در برابر JSON Schema اعتبارسنجی می‌شود؛ در صورت خطا، retry یا reject |
| ثبت ممیزی | هر اجرای AI در `ai_run` (ورودی، خروجی، تأییدکننده) ذخیره می‌شود |

### ۱۶.۳ تسک‌های AI و قرارداد خروجی

| تسک | ورودی (قطعی) | خروجی (JSON) |
|---|---|---|
| `daily_briefing` | آمار روز + لیست slot خالی + مشتریان at_risk | `{summary, top_actions[3], risks[]}` |
| `at_risk_actions` | لیست مشتریان at_risk + ترجیحات | `{customer, priority, suggested_message}[]` |
| `rebooking_suggestions` | مشتریان داخل پنجره + slotهای آزاد | `{customer, slot, message}[]` |
| `slot_fill_suggestions` | slotهای آزاد + صف انتظار | `{slot, ranked_customers[3]}[]` |
| `campaign_recommendation` | داده‌ی تقاضا بر اساس ساعت/روز | `{audience, offer, channel, timing}` |

نمونه‌ی Schema (برای `daily_briefing`):

```json
{
  "summary": "string",
  "top_actions": [
    { "priority": 1, "action": "string", "reason": "string", "audience_size": 0 }
  ],
  "risks": [
    { "customer_id": "string", "risk_score": 0.0, "suggested_action": "string" }
  ]
}
```

### ۱۶.۴ نمونه‌ی خروجی مطلوب (همان‌روح v1.1، اکنون قابل‌اجرا)

```text
امروز ۴ slot خالی دارید (سه‌شنبه ۱۴ تا ۱۶).
۱۲ مشتری نزدیک به زمان بازگشت هستند؛ ۶ مشتری در معرض ریزش.
پیشنهاد: ابتدا به ۶ مشتری at_risk و سپس به ۶ مشتریِ منطبق با slotهای خالی پیام ارسال شود.
```

---

## 17. گزارش صبحگاهی مالک

هر روز صبح (مثلاً ۰۸:۰۰ به وقت تهران) تولید و ارسال می‌شود.

```text
نوبت‌های امروز: ۲۳
Slotهای خالی: ۳
در انتظار تأیید: ۴
مشتریان در معرض ریزش: ۷
مشتریان جدید: ۴
درآمد مورد انتظار: ۴٬۶۰۰٬۰۰۰ تومان

Top 3 اقدامات:
۱. تأیید/ارسال پیشنهاد به ۶ مشتری at_risk (در انتظار تأیید شما)
۲. پر کردن ۳ slot خالی از صف انتظار
۳. پیگیری ۴ رزرو در انتظار تأیید پرداخت
```

---

## 18. معماری سیستم

> **اصلاح نسبت به v1.1:** دیاگرام قبلی خطی و بدون حلقه‌ی بازگشت به CRM بود. معماری اکنون **رویدادمحور** است و چرخه‌ی عمر مشتری را منعکس می‌کند.

### ۱۸.۱ دیاگرام رویدادمحور

```text
                 ┌──────────────────────────────────────────────┐
                 │              کلاینت‌ها                        │
                 │   PWA مشتری │ وب‌سایت │ پنل مدیریت (داشبورد)   │
                 └──────────────────────┬───────────────────────┘
                                        │ HTTPS
                 ┌──────────────────────▼───────────────────────┐
                 │         API Gateway + احراز هویت (RBAC)      │
                 └──────────────────────┬───────────────────────┘
                                        │
          ┌─────────────────────────────┼─────────────────────────────┐
          ▼                             ▼                             ▼
  ┌───────────────┐           ┌──────────────────┐          ┌────────────────┐
  │  Booking      │◄─────────►│   Availability   │          │   Billing      │
  │  (نوبت‌دهی)   │           │   (تقویم/ظرفیت)  │          │   (پرداخت)     │
  └──────┬────────┘           └──────────────────┘          └───────┬────────┘
         │                                                          │
         │            ┌──────────────────────────┐                  │
         └───────────►│        Customer CRM      │◄─────────────────┘
                      │  (tier + lifecycle + risk)│
                      └────────────┬─────────────┘
                                   │ رویدادها (appointment.completed, …)
                      ┌────────────▼──────────────────────────┐
                      │            Event Bus / صف (BullMQ)     │
                      └───────┬──────────────────┬────────────┘
                              │                  │
              ┌───────────────▼──────┐   ┌───────▼───────────────┐
              │   Automation Engine   │   │   Analytics / BI      │
              │ (یادآوری/لغو/waitlist)│   │ (SQL، گزارش قطعی)     │
              └───────────┬──────────┘   └───────────┬───────────┘
                          │                          │
              ┌───────────▼──────────────────────────▼───────────┐
              │                 Intelligence / AI                 │
              │        (اولویت‌بندی + پیش‌نهاد + روایت)           │
              └───────────────────────┬──────────────────────────┘
                                      │ تأیید انسانی
              ┌───────────────────────▼──────────────────────────┐
              │                 Owner Dashboard                   │
              │        (تأیید پیش‌نهادها، تصمیم نهایی)            │
              └──────────────────────────────────────────────────┘

        حلقه‌ی بازگشت: نتیجه‌ی هر رویداد (مراجعه، no-show، لغو، بازگشت)
        دوباره وارد CRM می‌شود و چرخه‌ی عمر مشتری را به‌روز می‌کند.
```

### ۱۸.۲ لایه‌ها

| لایه | مسئولیت |
|---|---|
| Presentation | PWA / وب / داشبورد |
| API / Auth | احراز هویت، RBAC، محدودسازی نرخ |
| Domain Services | Booking، Availability، CRM، Billing |
| Event Bus | انتشار/اشتراک رویدادها (BullMQ + PostgreSQL) |
| Automation Engine | واکنش به رویدادها + زمان‌بندی (Cron) |
| Analytics / BI | گزارش‌های قطعی |
| AI | اولویت‌بندی/پیشنهاد روی خروجی BI |
| Persistence | PostgreSQL (منبع حقیقت) |

### ۱۸.۳ آداپتورها (Interface)

هسته به هیچ سرویس خاصی وابسته نیست؛ اینها فقط Interface هستند:

```text
database    (PostgreSQL — پیش‌فرض)
calendar    (تقویم جلالی / تعطیلات)
messaging   (SMS: کاوه‌نگار/ملی‌پیامک؛ Telegram Bot)
payments    (IPG شاپرک / PSP)
analytics   (SQL/BI داخلی)
ai          (LLM API + اعتبارسنجی Schema)
```

---

## 19. چندمستأجری و پیکربندی

> **اصلاح نسبت به v1.1:** پیکربندی کامل شد (تعطیلات، واحد پول، سیاست‌های رزرو، برندینگ و …).

### ۱۹.۱ پیکربندی کامل یک آرایشگاه

```javascript
config: {
  business: {
    id: "shop-001",
    name: "نام آرایشگاه",
    city: "شهر",
    phone: "شماره",
    bookingUrl: "لینک رزرو",
    currency: "IRR",              // واحد پول (نمایش: تومان)
    timezone: "Asia/Tehran",
    calendar: "jalali",
    branding: { primaryColor: "#0f766e", logo: "..." }
  },
  workingHours: {
    // کلید روز هفته‌ی جلالی (0=شنبه ... 6=جمعه)
    saturday:   { open: "09:00", close: "21:00" },
    friday:     { closed: true }
  },
  holidays: ["1405/01/01", "..."],           // تعطیلات رسمی + خاص
  scheduling: {
    slotGranularityMinutes: 15,
    defaultBufferMinutes: 5,
    minLeadMinutes: 30,
    maxAdvanceDays: 30,
    minNoticeCancellationHours: 3,
    noShowGraceMinutes: 15
  },
  customer: {
    vipVisitThreshold: 8,
    churnGraceDays: 14,
    churnDays: 180
  },
  payment: { mode: "pay_at_visit" },         // pay_at_visit | prepayment | full_online
  messaging: {
    smsProvider: "kavenegar",
    telegramBotToken: "…",                    // اختیاری
    templates: { confirm: "…", remind_24h: "…", /* … */ }
  },
  retention: {
    rebookingWindowDelta: "auto",             // δ = max(2, round(0.1·m))
    waitlistOfferExpiryMinutes: 120,
    waitlistMaxOffersPerSlot: 3,
    waitlistMaxOffersPerCustomerPerDay: 1
  }
}
```

### ۱۹.۲ قواعد چندمستأجری

- هر جدول دارای `tenant_id`؛ تمام کوئری‌ها فیلتر tenant دارند (Row-Level Security توصیه‌شده).
- پیکربندی `versioned` است؛ تغییرات قابل بازگشت.

---

## 20. نقش‌ها و احراز هویت

> **اصلاح نسبت به v1.1:** نقش‌ها و احراز هویت کاملاً غایب بودند.

### ۲۰.۱ نقش‌ها و مجوزها (RBAC)

| مجوز | مالک | پذیرش | آرایشگر | مشتری |
|---|---|---|---|---|
| مشاهده‌ی داشبورد BI/AI | ✅ | — | — | — |
| تأیید پیشنهادهای AI | ✅ | — | — | — |
| رزرو دستی | ✅ | ✅ | — | — |
| ویرایش نوبت‌ها | ✅ | ✅ | — | (فقط نوبت خود) |
| چک‌این / شروع / پایان | ✅ | ✅ | ✅ | — |
| مشاهده‌ی تقویم کامل | ✅ | ✅ | — | — |
| مشاهده‌ی تقویم خود | — | — | ✅ | — |
| پیکربندی آرایشگاه | ✅ | — | — | — |
| مشاهده‌ی شماره‌ی مشتری | ✅ | ✅ | ✅ | — |

### ۲۰.۲ احراز هویت

- **مشتری:** ورود با **OTP موبایل** (بدون رمزعبور)؛ توکن با انقضا + محدودسازی نرخ درخواست OTP.
- **کارکنان/مالک:** ایمیل/موبایل + رمزعبور (هش‌شده با Argon2/bcrypt) + احراز دومرحله‌ای اختیاری.

### ۲۰.۳ جلوگیری از پروفایل تکراری

- شماره‌ی موبایل نرمال‌سازی می‌شود (`+98912…` و `0912…` یکسان) و یکتاست.
- هنگام ورود OTP، اگر شماره موجود باشد، به پروفایل همان مشتری متصل می‌شود (merge امن).

---

## 21. مهاجرت داده و راه‌اندازی

> **اصلاح نسبت به v1.1:** هیچ آرایشگاهی از صفر شروع نمی‌کند؛ import داده اضافه شد.

### ۲۱.۱ راه‌اندازی آرایشگاه جدید

1. ایجاد `tenant` با پیکربندی (بخش ۱۹).
2. ثبت خدمات، آرایشگران، ساعات کاری و تعطیلات.
3. (اختیاری) import مشتریان از CSV/Excel.

### ۲۱.۲ قالب import مشتریان

```csv
mobile,name,gender,notes
09121112233,علی رضایی,,
09124445566,حسین کریمی,,
```

- dedupe بر اساس موبایل نرمال‌شده؛
- سوابق تاریخی در صورت وجود، به‌عنوان `visit`های `completed` برای محاسبه‌ی median اولیه وارد می‌شوند (بدون‌آن، Risk Score تا ۳ مراجعه‌ی واقعی «نامشخص» می‌ماند).

---

## 22. امنیت و حریم خصوصی

- **رمزنگاری:** PII حساس در حالت استراحت رمزنگاری می‌شود؛ TLS برای همه‌ی ارتباطات.
- **حداقل‌سازی داده:** فقط شماره، نام و سوابق مراجعه؛ داده‌ی اضافه جمع‌آوری نشود.
- **محدودسازی نرخ:** روی OTP، فرم رزرو و API عمومی.
- **ممیزی:** `event_log` و `ai_run` تغییرات مهم را ثبت می‌کنند.
- **حذف داده:** قابلیت حذف پروفایل به‌درخواست مشتری.

---

## 23. کیفیت، پشتیبان‌گیری و استقرار

> **اصلاح نسبت به v1.1:** تست، بکاپ و استقرار در هیچ فازی نبودند.

| حوزه | سیاست |
|---|---|
| تست | واحد (قواعد رزرو، ماشین حالت، فرمول Risk)، یکپارچگی (اتوماسیون/پیامک)، E2E (رزرو تا تکمیل) |
| پشتیبان‌گیری | بکاپ روزانه‌ی PostgreSQL + Point-in-Time Recovery |
| مانیتورینگ | آپتایم، عمق صف، نرخ تحویل SMS، خطاهای پرداخت |
| هشدار | خطای پرداخت، شکست پیام، عقب‌ماندن صف اتوماسیون |
| استقرار | Docker + CI/CD؛ محیط‌های dev/staging/prod |

---

## 24. پشته‌ی فنی (Stack)

| لایه | انتخاب پیشنهادی |
|---|---|
| Frontend | PWA (React + Vite / Next.js)، فونت وزیرمتن، RTL |
| آفلاین | Service Worker + IndexedDB (مشاهده‌ی نوبت‌ها و رزرو آفلاین با صف همگام‌سازی) |
| Backend | Node.js (Fastify/NestJS)، REST API |
| دیتابیس | PostgreSQL (تضمین‌های همزمانی بخش ۶) |
| صف/زمان‌بندی | BullMQ + Redis، Cron (به وقت تهران) |
| اتوماسیون | Custom Queue + n8n (اختیاری برای فلوی کسب‌وکار) |
| پیام‌رسانی | Adapter SMS (کاوه‌نگار/ملی‌پیامک) + Telegram Bot |
| پرداخت | Adapter IPG شاپرک |
| AI | LLM API + خروجی JSON + اعتبارسنجی Schema |
| مانیتورینگ | Sentry + Prometheus/Grafana |
| استقرار | Docker، GitHub Actions |

---

## 25. نقشه‌ی راه

> **اصلاح نسبت به v1.1:** شکاف‌ها (پرداخت، داشبورد مدیریت، احراز هویت، تست، بکاپ، استقرار) پر شد.

### Phase 0 — زیرساخت (Foundation)
- Schema دیتابیس + `tenant` + تقویم جلالی
- احراز هویت و RBAC
- CI/CD، محیط‌ها، بکاپ
- Adapterهای خالی (database/calendar/messaging/payments/analytics/ai)

### Phase 1 — MVP
- خدمات، آرایشگران، ساعات کاری، تعطیلات
- تقویم + رزرو آنلاین + رزرو دستی (با تضمین همزمانی)
- دیتابیس مشتری + تأیید نوبت

### Phase 2 — اتوماسیون
- یادآوری ۲۴/۳ ساعت، لغو، No-Show، تاریخچه‌ی مشتری
- Waitlist و بازیابی slot
- اتصال SMS (و Telegram Bot)

### Phase 3 — Retention
- Predictive Rebooking، تشخیص at-risk، Reactivation
- Referral، VIP

### Phase 4 — هوش
- BI و KPI (گزارش روزانه/هفتگی)
- AI Business Assistant + Daily Briefing
- پیشنهاد کمپین، بهینه‌سازی ظرفیت و درآمد

### Phase 5 — مقیاس
- Onboarding خودکار (import) و White-label
- پرداخت آنلاین کامل (پیش‌پرداخت/آنلاین) — در صورت نیاز بازار
- اپ استور/ساخت PWA نصب‌پذیر برای مشتری

---

## 26. پیوست‌ها

### ۲۶.۱ وضعیت‌های نوبت (enum)

```text
requested | confirmed | checked_in | in_progress | completed | cancelled | no_show
```

### ۲۶.۲ وضعیت‌های چرخه‌ی عمر مشتری (enum)

```text
new | active | at_risk | inactive | churned
```

### ۲۶.۳ سطح مشتری (enum)

```text
regular | vip
```

### ۲۶.۴ رویدادها (enum)

```text
appointment.created | appointment.confirmed | appointment.cancelled
appointment.checked_in | appointment.completed | appointment.no_show
appointment.rescheduled | customer.vip_upgraded | customer.at_risk_detected
customer.reactivation | slot.released | waitlist.offer_sent
payment.succeeded | referral.rewarded
```

### ۲۶.۵ خلاصه‌ی تغییرات نسبت به v1.1.0

| حوزه | وضعیت v1.1 | اصلاح v2.0 |
|---|---|---|
| حالت نوبت | تعریف نشده | ماشین حالت + جدول گذار (بخش ۵) |
| ظرفیت | فقط ساعات کاری | سه‌لایه + مدت خدمت + بافر + تعطیلات (بخش ۴) |
| Double Booking | جمله | ایندکس یکتا + تراکنش + idempotency (بخش ۶) |
| وضعیت مشتری | تک‌بُعدی | `tier` + `lifecycle` با قواعد گذار (بخش ۷) |
| Risk Score | عدد بدون فرمول | فرمول + داده‌ی کم + تعدیل‌گر (بخش ۸) |
| Rebooking | مثال بدون قاعده | فرمول پنجره (بخش ۹) |
| Waitlist | فهرست معیار | وزن + انقضا + سقف (بخش ۱۰) |
| No-Show | فقط اسم | تعریف + عواقب + اثر تحلیلی (بخش ۱۱) |
| Referral | فقط اسم | مکانیزم کامل (بخش ۱۲) |
| پرداخت | غایب در فازها | مدل + درگاه + ریفاند (بخش ۱۳) |
| پیام‌رسانی | فقط متغیرها | ماتریس + idempotency + تایم‌زون + کانال ایران (بخش ۱۴) |
| AI | بدون گارد | گارد + تفکیک BI + JSON Schema (بخش ۱۶) |
| معماری | خطی بدون حلقه | رویدادمحور با حلقه‌ی بازگشت (بخش ۱۸) |
| پیکربندی | ناقص | کامل (بخش ۱۹) |
| نقش‌ها/احراز هویت | غایب | RBAC + OTP + dedupe (بخش ۲۰) |
| مهاجرت داده | غایب | import CSV + median اولیه (بخش ۲۱) |
| نقشه‌ی راه | شکاف‌دار | کامل (بخش ۲۵) |
